import React from "react";
import compose from "../utils/compose";
import withMenu from "../lib/withMenu";

const SelectionSort = props => {
  return <div>Selection Sort</div>;
};

export default compose(withMenu)(SelectionSort);
